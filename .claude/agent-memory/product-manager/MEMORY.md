# Product Manager Agent Memory

- [opcflow artifact registration](opcflow-artifact-registration.md) — `output` is create-only; edits auto-refresh content_hash; use `scan`/`sync` to confirm, don't re-`output`
